DISCORD_TOKEN=

# Twitter API Bearer Token (Optional - for Twitter features)
TWITTER_BEARER_TOKEN=your_twitter_bearer_token_here

# ==================== BOT SETTINGS ====================
# Check interval in seconds (default: 30)
CHECK_INTERVAL_SECONDS=30

# Data files
SUBS_FILE=subscriptions.json
CONFIG_FILE=bot_config.json
STATS_FILE=bot_stats.json

# Notification sound file (must be in bot directory)
NOTIFICATION_SOUND=notif.mp3

# Default language (tr or en)
DEFAULT_LANGUAGE=tr

# Test mode - prints extra debug info (true/false)
TEST_MODE=false

# ==================== OPTIONAL: FIREFOX CONFIG ====================
# If you have a custom Firefox path, uncomment and set:
# FIREFOX_BINARY_PATH=/usr/lib/firefox-esr/firefox-esr

# ==================== NOTES ====================
# 1. Get Discord Token: https://discord.com/developers/applications
#    - Create application → Bot → Copy Token
#    - Enable: MESSAGE CONTENT INTENT, GUILD INTENTS
#
# 2. Get Twitter Bearer Token: https://developer.twitter.com/
#    - Create project → Generate Bearer Token
#    - Optional - only needed for Twitter features
#
# 3. Notification Sound:
#    - Place your notif.mp3 in the bot directory
#    - FFmpeg must be installed for voice features
#    - Install: sudo apt install ffmpeg
#
# 4. Recommended CHECK_INTERVAL_SECONDS:
#    - 30 seconds: Fast but more API calls
#    - 60 seconds: Balanced
#    - 300 seconds: Conservative (5 minutes)